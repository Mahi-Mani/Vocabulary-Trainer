  <!-- Optional JavaScript -->
    <!-- jQuery first, then Popper.js, then Bootstrap JS -->
    {{!-- <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script> --}}
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
  
  {{!-- Jquery --}}
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>

  {{!-- Code for header background --}}
   <div id="bg" class="py-3 text-center" style="background-image: url(https://ichef.bbci.co.uk/news/912/cpsprodpb/179AE/production/_103568669_gettyimages-579236076.jpg); border: 1px solid black;"> 
  {{!-- Jumbotron --}}
    <div class="container">
    <div class="row">
    <div class="col-md-2"></div>
    <div class="col-md-8">
    <div class="jumbotron">
    <h1 class="display-4">Vocabulary Builder</h1>
    <p class="lead">Build Your Vocabs here!</p>
    <hr class="my-4">
    {{!-- Text area --}}
    <div class="form-group">
    <label for="exampleFormControlTextarea1">Click on submit to add word to your vocabs</label>
    <textarea class="form-control" id="input-word" rows="2"></textarea>
  </div>

    {{!-- <a class="btn btn-primary btn-lg" href="#" role="button"id="submit-btn">Submit</a> --}}
    <button type="button" class="btn btn-primary"id="submit-btn">Add Word</button>
    <button type="button" class="btn btn-primary"id="view-btn">View all Words</button>
    </div>
    </div>
    <div class="col-md-2"></div>
    </div>

    {{!-- Table1 --}}
    {{!-- Row --}}
    <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8">
    <table class="table table-dark d-none"id="wordTable">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Word</th>
      <th scope="col">Mastered</th>
    </tr>
  </thead>
  <tbody>
    <td>
    {{#each data}}
      {{#unless MASTERED}}
      {{> vocabs/vocab-block MASTERED=true}}
      {{/unless}}
  {{/each}}
    </td>
  </tbody>
</table>
</div>
<div class="col-md-2"></div>
</div>

{{!-- Table2 --}}
    {{!-- Row --}}
    <div class="row">
        <div class="col-md-2"></div>
        <div class="col-md-8">
    <table class="table table-dark d-none"id="masteredTable">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Word</th>
      <th scope="col">Mastered</th>
    </tr>
  </thead>
  <tbody>
    <td>
    {{#each data}}
      {{#unless MASTERED}}
      {{> vocabs/vocab-block MASTERED=true}}
      {{/unless}}
  {{/each}}
    </td>
  </tbody>
</table>
</div>
<div class="col-md-2"></div>
</div>
    </div>

    {{!-- Javascript --}}
    <script>
        // Submit button to add a new word
        $("#submit-btn").on("click", function(event){
            // To prevent page from loading
            event.preventDefault();

            // Grabbing hold of value from user input
            var newWord = {
                    word: $("#input-word").val().trim()
                    };
                    console.log(newWord.word);

            // Post values
            $.ajax("/api/words", {
                type: "POST",
                data: newWord
            }).then(function(){
                console.log("Created new word");
                // To reload the page
                // location.reload();
            })
        })

    // View all words
    $("#view-btn").on("click", function(){
        console.log("View all button clicked");
        $.get("/api/all/words", function(data){
            console.log("Inside get of all words");
            console.log(data);
            for(var i=0; i<data.length; i++){
              if(!(data[i].MASTERED)){
            createTable(data[i]);
              }
              else{
                createMasteredTable(data[i]);
              }
            }
        })
    })

    // Function to create table for all words
    function createTable(data){
        $("#wordTable").removeClass("d-none");

    // Create table row and table data for all values
    var tr = $("<tr>");
    tr.append("<th>" + data.ID + "</th>");
    tr.append("<td>" + data.WORD + "</td>");
    tr.append("<td>" + data.MASTERED + "</td>");
    // TRY RENDERING THIS VALIE TO HANDLEBAR
    tr.append("<td>" +  "<button class='mastered' data-id=data[i].ID> LEARNING </button>" + "</td>");
    $("#wordTable").append(tr);
    }

    function createMasteredTable(data){
      console.log("inside master table");
           $("#masteredTable").removeClass("d-none");

    // Create table row and table data for all values
    var tr = $("<tr>");
    tr.append("<th>" + data.ID + "</th>");
    tr.append("<td>" + data.WORD + "</td>");
    tr.append("<td>" + data.MASTERED + "</td>");
    // TRY RENDERING THIS VALIE TO HANDLEBAR
    tr.append("<td>" +  "<button class='mastered' data-id=data[i].ID> MASTERED </button>" + "</td>");
    $("#masteredTable").append(tr);
    }
    </script>